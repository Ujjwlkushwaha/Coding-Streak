  // int curr = 0, next = curr + 1;

    // while (curr < n and next < n)
    // {
    //     if (arr[curr] == 0 and arr[next] != 0)
    //     {
    //         swap(arr[curr], arr[next]);
    //         curr++, next = curr + 1;
    //     }
    //     else if (arr[curr] != 0 and arr[next] != 0)
    //     {
    //         curr++, next++;
    //     }
    //     else if (arr[curr] == 0 and arr[next] == 0 )
    //     {
    //         next++;
    //     }
    // }